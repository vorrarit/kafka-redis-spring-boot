pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building ...'
                cd message-producer
                sh '/opt/gradle/gradle-6.3/bin/gradle bootJar'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying ...'
            }
        }
    }
}